export interface Flight {
    origin: string,
    destination: string,
    departureDate: string,
    returnDate: string,
    numberOfStops: number,
    passengerNum: number,
    currency: string,
    totalPrice: number
}