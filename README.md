# flight-scanner

**Tech stack
* .NET Core 2.1
* Angular 10.0.1

**Setting up and run
1. in flight-scanner-client folder run **npm i** in terminal
2. change proxy url in _src/configuration/proxy.conf.json_ to .net Core localhost url
3. Run client application
4. Run server side application